[package]
name = "automata-cli"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "automata-cli"
path = "src/main.rs"

[dependencies]
automata-core = { path = "../automata-core" }
clap = { version = "4", features = ["derive"] }
